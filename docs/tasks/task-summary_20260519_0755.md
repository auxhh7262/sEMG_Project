# Task Summary - 2026-05-19 校准页UI修复

## Objective
修复校准页(calibrate)两个UI问题：(1) 删除红圈2的重复按钮/提示区 (2) 整合散落的校准流程/方法/注意事项为单一说明卡片

## Changes
### index.wxml
- 删除 `main-area` 整个block（重复的主按钮+曲线库，即红圈2）
- 删除 `calib-footer` 区块（"关于校准"冗余文字）
- 重写 `intro-card` 为整合版"校准说明"：3步骤+每步详细描述(新增intro-detail)+注意事项

### index.wxss
- 新增 `.intro-detail` 样式（22rpx, #888, 左缩进48rpx）
- `.intro-step` 字号加大、颜色加亮（更突出标题行）
- 删除 `.calib-footer` / `.footer-section` / `.footer-title` / `.footer-text` 样式

## Outcome
编译通过 ✅ idle页面不再有重复按钮和散落说明
