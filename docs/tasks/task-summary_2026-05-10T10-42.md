# 任务摘要 2026-05-10T10:42

## 目标
对微信小程序 sEMG 设备 WiFi 配网页面进行 UI 调整和 TabBar 重新排序。

## 完成的修改

### 1. TabBar 顺序调整
**文件**: `app.json` + `custom-tab-bar/index.js`

| 原顺序 | 新顺序 |
|--------|--------|
| 实时监测 | 校准建库 |
| 校准建库 | 实时监测 |
| 数据分析 | 数据分析 |
| 设置 | 配置网络 |

- `app.json` 的 pages 数组和 tabBar.list 同步调整
- `custom-tab-bar/index.js` 的 list 和 routes 数组同步调整

### 2. 配置网络页 WXML 重构
**文件**: `pages/index/index.wxml`

- ✅ 移除 result-banner（右上角配网成功提示横幅），用 `{{statusText}}` 的 section-badge 代替即可
- ✅ "已连接网络" → "sEMG手环已连接网络"
- ✅ IP 前加 "IP："前缀：`IP：{{deviceIp}}`
- ✅ 配网说明区块移至重置按钮下方，作为独立 section
- ✅ "发送配置" → "配置网络"

### 3. 样式微调
**文件**: `pages/index/index.wxss`

- 第一个表单 field-label 增加 `padding-top: 12rpx`（修复"WiFi 名称"中"W"显示不完整的问题）

## 待测试项
- [ ] 模拟器预览，确认"W"字符显示完整
- [ ] 配网成功后的 IP 卡显示格式
- [ ] TabBar 四个按钮顺序和图标是否正确
- [ ] "配网说明"区块在重置按钮下方后是否美观
