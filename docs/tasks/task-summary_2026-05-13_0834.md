# Task Summary: sEMG代码审查Bug修复批次1

## Objective
基于全面代码审查发现的🔴🟡级问题，按优先级进行修复。

## Key Reasoning
1. **"start"命令语义混淆**是核心风险：校准页和监测页都发`{"cmd":"start"}`，但语义完全不同。采用"新增start_calib + start按状态分流"方案，既修复问题又保持向后兼容。
2. **tcpClient.js参数不一致**：直接对齐到const.js的值（心跳5s、重连2s），const.js作为唯一配置源。
3. **StateMachine缺ST_DB_FEATURE**：AppController已绕过验证直接转换，必须补全转换规则。
4. **日志刷屏**：sendData在10Hz下每秒20行LOG，改为2秒1次摘要。

## Completed Fixes (8 items)
1. 🔴 phone-net-card重复删除 (index.wxml)
2. 🔴 start/start_calib命令分离 (ProtocolHandler.cpp + calibrate/index.js + const.js)
3. 🔴 心跳5s/重连2s统一 (tcpClient.js)
4. 🟡 ST_DB_FEATURE状态转换补全 (SystemStateMachine.cpp)
5. 🟡 sendData日志限频2秒/次 (NetManager.cpp)
6. 🟡 totalMinutes修正为分钟 (analysis/index.js)
7. 🟡 const.js CALIB参数清理+重命名 (const.js)
8. 🟡 校准页3处start→start_calib (calibrate/index.js)

## Build Status
- ✅ Firmware compiled: RAM 69.7% (22848/32768), Flash 57.5% (150720/262144)
- Pending: device upload (COM4 not available)

## Remaining (not fixed yet)
- DHCP 5s blocking → needs non-blocking state machine refactor
- handleSaveRecord heap allocation → needs static buffer
- Calibration push f/q/a all zero → needs protocol extension
- RMS normalization hardcoded → needs calibration data
