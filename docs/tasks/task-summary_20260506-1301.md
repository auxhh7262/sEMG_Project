# Task Summary: sEMG Firmware 全层代码审查与P0修复

**时间**: 2026-05-06 12:45~13:01
**项目**: E:\Personal\sEMG\sEMG_Firmware_V1.4

## 目标
对0_Base~5_AppController全层代码进行审查，修复P0级别问题。

## 审查发现（按严重程度）

### P0（已修复）
| # | 问题 | 修复内容 |
|---|------|---------|
| S0-1 | `StorageManager::Init()` 栈上分配8KB(`AZone_Sector_t sector[2]`)，32KB RAM MCU极易栈溢出 | 改为逐扇区读取，复用唯一静态4KB镜像 `g_currentAZoneMirror` 做工作缓冲；删除冗余 `g_sectorWorkBuf` |
| A0-1 | `_handleCalibRestState`/`_handleCalibMaxState` 完全空壳，校准流程无法运行 | 实现完整校准状态机：REST阶段10Hz采样→超时切MAX→MAX阶段采样→超时验证→成功转MONITORING/失败转ERROR |
| A0-2 | `_handleMonitoringState` 每100ms调用update()含FFT(需256ms数据)，用0值污染质量评分 | SignalProcessor::update()增加短路保护：数据不足直接返回0.0f，不执行FFT/质评；AppController中跳过rms=0的发送 |
| C0-1 | `validateResult()` MDF反向检查容忍度1.5x过大，会放过错误校准 | 从1.5x收紧到1.1x，允许10%测量噪声容差 |

### P1（本轮未修，记录备查）
| # | 问题 | 备注 |
|---|------|------|
| N0-1 | `NetManager::_tryConnectWifi` WiFi.begin()后立即return true，连接状态误判 | WiFi连接是异步的 |
| S0-2 | CRC16和AZone校验空壳→本轮已实现_calcCRC16和_validateAZoneSector | ✅附带给修了 |
| B0-5 | `StateManager::getCalibProgress()` 返回值可能溢出 | `millis()*100`溢出 |
| C0-2 | addSample样本溢出无声丢失 | MAX_SAMPLES时无日志 |

### 附带给修的
- **S0-2**: 实现了 `_calcCRC16`（Modbus CRC16）、`_validateAZoneSector`（magic+CRC校验）、`_getAZoneSectorAddr`、`_writeMultiPage`（跨页写入）
- **A0-3**: 补全 `onCommandReceived` 缺失的 CMD_STOP、CMD_VERIFY_CALIB 处理
- **WiFi凭证存储**: 改用SPI Flash A区reserved空间末尾96字节存储（原TODO空壳），无需EEPROM库

## 修改文件清单
| 文件 | 修改内容 |
|------|---------|
| `src/3_Storage/StorageManager.cpp` | Init()栈溢出修复、CRC16/校验/写入实现、WiFi凭证Flash存储 |
| `src/5_AppController/AppController.cpp` | 校准状态机实现、Monitoring短路保护、命令处理补全 |
| `src/1_Signal/SignalProcessor.cpp` | update()增加数据不足短路返回 |
| `src/2_Calibration/CalibrationManager.cpp` | MDF反向检查从1.5x收紧到1.1x |

## 编译结果
- RAM: 60.1% (19684/32768)
- Flash: 45.9% (120252/262144)
- 状态: ✅ 编译通过
