# 任务摘要 2026-05-10T10:58

## 目标
调整网络配置页面布局：
1. 配网说明始终显示（移除条件限制）
2. 三个按钮一排：扫描设备 | 配置网络 | 重置网络
3. IP信息移至按钮上方
4. 重置提示合并到配网说明

## 完成的修改

### 1. 页面结构重构
**文件**: `pages/index/index.wxml`

```
新布局:
  WiFi 名称/密码输入框
  2.4GHz 提示
  [IP 卡片] ← 配网成功后显示，位于按钮上方
  [扫描设备] [配置网络] [重置网络] ← 三按钮一排
  配网说明（始终显示，包含重置提示）
```

### 2. 三按钮布局
**文件**: `pages/index/index.wxss`

新增 CSS:
- `.btn-group-three`: 三按钮容器，flex 布局
- `.btn-item`: 统一按钮样式，高度 72rpx
- `.btn-item.btn-accent`: 配置网络按钮（绿色）
- `.btn-item.btn-reset`: 重置网络按钮（红色边框）
- `.btn-item.btn-disabled`: 禁用态（opacity: 0.4）

### 3. 配网说明调整
- 移除 `wx:if="{{!deviceIp}}"` 条件，始终显示
- 新增第5条：重置网络提示（⚠ 图标）

### 4. 按钮命名
- "重置" → "重置网络"

## 待测试
- [ ] 三按钮一排是否正确显示
- [ ] IP 卡片在按钮上方是否美观
- [ ] 配网说明是否始终可见
- [ ] 重置网络按钮禁用态是否正常（无 bleConnected/deviceIp 时）