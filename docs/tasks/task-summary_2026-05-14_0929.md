# task-summary_2026-05-14_0929

## 目标
1. 分析 v3.9.9 校准结果
2. REST 阶段增加倒计时显示（与 MAX 一致）

## 关键推理
v3.9.9 校准日志：sr=1000 全程稳定，MDF 计算完全正常 ✅
但校准失败：REST RMS=12.24mV > MAX RMS=10.95mV（ref not 2x rest）
用户确认传感器已贴肌肉且用力收缩，但 RMS 仍然过低（正常应 100-300mV）

## 修改
- `mini_program/pages/calibrate/index.js`:
  - `startCalibration()` 不再先做10秒准备倒计时，直接调用 `_beginCollecting('resting')`
  - `_beginCollecting('resting')` 设置 countdown=10 并启动倒计时
  - REST 阶段现在与 MAX 一致：倒计时 + 进度圆环 + 实时数值
- `mini_program/pages/calibrate/index.wxml`:
  - 进度圆环条件增加 `phase === 'resting'`

## 待观察
- RMS 过低问题（可能传感器接触/接线问题）
