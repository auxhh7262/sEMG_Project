# 任务摘要 2026-05-06 13:50 — StorageManager 完整实现

## 目标
完成 StorageManager 所有存根函数的实现，包括 S0-2 (CRC16/AZone校验)、B区业务逻辑、C区业务逻辑。

## 关键修改

### 文件: `src/3_Storage/StorageManager.cpp` (完整重写)

**1. S0-2 修复 — CRC16 校验完整实现**
- `_calcCRC16()`: CRC16-Modbus 算法（多项式0xA001），非空壳
- `_validateAZoneSector()`: 完整magic+CRC16双重校验
- `UpdatePersonalCalib()`: 双缓冲交替写 + CRC16计算 + 回读验证

**2. B区完整业务逻辑**
- `BZone_GetNextAvailableSlot()`: 扫描49个槽位的record_status找EMPTY
- `BZone_BeginRecord()`: 初始化写上下文 + 擦除槽位(10个4KB扇区) + 写header
- `BZone_AppendFeatureSequence()`: RMS/MDF序列流式写入
- `BZone_AppendRawWaveform()`: 4相原始波形流式写入
- `BZone_EndRecord()`: CRC16校验 + footer提交 + 状态标记VALID
- 内部: `_bzone_stream_write()`, `_bzone_write_footer_and_commit()`, `_bzone_writeHeaderToSlot()`

**3. C区完整业务逻辑**
- `CZone_AppendDataPoint()`: 256字节RAM缓存 + 自动刷盘 + 块满自动分配新块
- `_czone_initFromScratch()`: 首次初始化，写CZBL块头
- `_czone_loadStateFromAZone()`: 从A区锚点恢复C区状态
- `_czone_flushCache()`: 缓存刷入Flash
- `_czone_allocateNewBlock()`: 分配新64KB块 + 环绕处理
- `_czone_finalizeCurrentBlock()`: 块完成时更新header

**4. WiFi 凭证 (RA4M1 Data Flash / EEPROM)**
- `LoadWifiCredentials()`: EEPROM读取 + magic验证
- `SaveWifiCredentials()`: EEPROM.update()减少磨损

**5. A区初始化优化 (S0-1 已在前次完成)**
- `Init()`: 双缓冲逐扇区验证 + 校准数据RAM缓存 + C区状态恢复

### 辅助函数
- `_writeMultiPage()`: Flash页对齐写入(256字节/页)
- `_getAZoneSectorAddr()`: A区扇区地址计算

## 编译结果
- RAM: 60.1% (19696/32768 bytes)
- Flash: 46.4% (121732/262144 bytes)
- 状态: ✅ SUCCESS

## 硬件参数依据 (W25Q128FV + RA4M1 数据手册)
- Flash: 16MB, 4096扇区(4KB), 页256B, 扇区擦除100-400ms, 64KB块擦除200-1000ms
- 擦写寿命: >100,000次, 数据保持>20年
- RA4M1: 32KB SRAM, 8KB Data Flash(EEPROM), Cortex-M4+FPU
