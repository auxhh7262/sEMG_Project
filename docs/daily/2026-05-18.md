

## 2026-05-18 16:20 - Phase 1 小程序代码写入完成

### 已完成文件
- [x] app.json → 5 TabBar（校准/建库/实时监测/数据分析/网络配置）
- [x] utils/storage.js → current_user工具（getCurrentUser/setCurrentUser/getSlotProfile/saveSlotProfile/getAgeGroup）
- [x] pages/calibrate/index.js → 重构（~300行，完整校准+曲线拟合）
- [x] pages/calibrate/index.wxml → 模板（profile卡片+4个phase+弹窗）
- [x] pages/calibrate/index.wxss → 精简样式
- [x] pages/calibrate/form.json → navigationBarTitleText="肌电校准"
- [x] pages/database/index.js → 新建（建库流程+记录列表+曲线库）
- [x] pages/database/index.wxml → 模板（菜单+表单+采集+标记+弹窗）
- [x] pages/database/index.wxss → 样式
- [x] pages/database/form.json → navigationBarTitleText="建库"

### 待完成
- [ ] 固件A区V1.2（StorageManager/CalibData结构体+has_curve/curve_coef字段）
- [ ] 固件：save_curve命令处理
- [ ] 小程序：bleClient.js加onCalibData/offCalibData/list_curves/delete_record/db_*命令
- [ ] 小程序：实时监测/数据分析页需改用A区has_curve判断算法
- [ ] 编译小程序预览码验证

### 关键实现细节
- 校准页 BLE命令：get_rest_ref → get_calib_result → save_calib → list_curves → save_curve
- 建库页 BLE命令：db_start → db_mark → db_save → list_records → delete_record → list_curves
- storage.js：current_user存wxStorage，槽位profile存slot0_profile/slot1_profile
- 曲线自动匹配：getAgeGroup(age)→0(<18)/1(18-35)/2(36-55)/3(56+)



## 2026-05-18 16:29 - Phase 1 小程序代码写入完成

### 已完成文件（10个）
- [x] app.json → 5 TabBar（校准/建库/实时监测/数据分析/网络配置）
- [x] utils/storage.js → current_user工具（getCurrentUser/setCurrentUser/getSlotProfile/saveSlotProfile/getAgeGroup）
- [x] pages/calibrate/index.js → 重构（~300行，完整校准+曲线拟合）
- [x] pages/calibrate/index.wxml → 模板（profile卡片+4个phase+弹窗）
- [x] pages/calibrate/index.wxss → 精简样式
- [x] pages/calibrate/form.json → navigationBarTitleText="肌电校准"
- [x] pages/database/index.js → 新建（建库流程+记录列表+曲线库）
- [x] pages/database/index.wxml → 模板（菜单+表单+采集+标记+弹窗）
- [x] pages/database/index.wxss → 样式
- [x] pages/database/form.json → navigationBarTitleText="建库"

### 待完成
- [ ] 固件A区V1.2（StorageManager/CalibData结构体+has_curve/curve_coef字段）
- [ ] 固件：save_curve命令处理
- [ ] 小程序bleClient.js：补onCalibData/offCalibData + list_curves/save_curve/delete_record + db_*命令
- [ ] 小程序：实时监测/数据分析页需改用A区has_curve判断算法
- [ ] 编译小程序预览码验证

### 核心设计
- 校准页BLE命令：get_rest_ref → get_calib_result → save_calib → list_curves → save_curve
- 建库页BLE命令：db_start → db_mark → db_save → list_records → delete_record → list_curves
- storage.js：current_user存wxStorage，槽位profile存slot0_profile/slot1_profile
- 曲线自动匹配：getAgeGroup(age)→0(<18)/1(18-35)/2(36-55)/3(56+)，gender+age_group+handedness精确匹配
- A区V1.2：2槽位×校准数据，has_curve(bool)+curve_coef[5]+curve_baseline_rms/mdf
- B区曲线库：curve_id 1-16（2性别×4年龄段×2利手），status=1可用


## 2026-05-18 16:36 - Phase 2 固件分析完成

### 固件命令处理架构（ProtocolHandler.cpp）

固件命令统一在 `ProtocolHandler::handleJsonCommand()` 路由：
- `query_cz` → `gAppController.handleQueryCZ()`
- `save_record` → `gAppController.handleSaveRecord()`
- `list_records` → `gAppController.handleListRecords()`
- `delete_record` → `gAppController.handleDeleteRecord()`
- `list_curves` → `gAppController.handleListCurves()`
- `gen_personal_curve` → `gAppController.handleGenPersonalCurve()`
- `get_curve` → `gAppController.handleGetCurve()`
- `start_db_feature` → `gAppController.handleStartDbFeature()`
- `capture_raw_phase` → `gAppController.handleCaptureRawPhase()`
- `raw_phase_done` → `gAppController.handleRawPhaseDone()`
- `save_calib` → `gStorage.UpdatePersonalCalib()` + `gSignal.setCalibration()`
- `start_calib` → `gAppController.onCommandReceived(CMD_START_CALIB)`
- `start` → IDLE时启动校准，MONITORING时发确认
- `stop` → `gAppController.onCommandReceived(CMD_STOP)`

固件响应用 `cmd` 字段（非 `type`），小程序需要适配。

固件 AZone 结构体已有 curve 字段（PersonalCalibData_t）：
- `has_curve` (uint8_t)
- `curve_coef[5]` (float[5])
- `curve_baseline_rms` / `curve_baseline_mdf` (float)

### 小程序 bleClient.js 改造（v2.3.0 新增）

**writeAndRead 命令-响应机制**（核心，8秒超时）：
- 发送 `{cmd, seq, payload}` → 等待响应 `{cmd:"ack", seq}` → resolve
- 内部 `_cmdPendingResolves[seq]` / `_cmdPendingRejects[seq]`
- 接收 JSON 响应时匹配 seq，自动 resolve/reject

**onCalibData 监听器**（用于校准页实时数据）：
- 监听 `type='calib_data'` 的 BLE notify
- 内部 `_calibDataListeners[]`

**扩展 _tryParseCompleteJson**：
- `data.cmd === 'ack'` && seq匹配 → resolve pending promise
- `data.type === 'calib_data'` → 通知 `_calibDataListeners`
- 已有 `cmd === 'status'` 解析

### 待完成

- [ ] 固件：save_curve 命令处理（A区写入 curve_coef）
- [ ] 小程序：bleClient.js 补全 `get_rest_ref`/`get_calib_result`/`db_start`/`db_mark`/`db_save` 命令封装
- [ ] 小程序：calibrate/index.js 接入 `onCalibData` 实时监听
- [ ] 小程序：database/index.js 实现建库状态机（调用 db_* 命令）
- [ ] 小程序：monitor/index.js 改用 A区 has_curve 判断算法
- [ ] 编译小程序预览码验证

### 设备信息（本次会话）
- 设备 IP：192.168.137.234（WiFi Direct USB以太网）
- WebSocket 端口：8888
- 电脑 IP：192.168.137.1
- 日志服务器：192.168.137.1:9876
- 串口：COM4 / 115200


## 2026-05-18 16:38 - ⚠️ 关键架构纠正：BLE vs WiFi 通道分离

### 正确的通信架构
- **BLE（bleClient.js）**→ 仅用于配网：`sendWifiConfig(ssid, password)` + `onIpReceived(callback)`，固件走 `BleConfigServer.cpp`
- **WiFi WebSocket（wifiClient.js）**→ 所有数据命令走这里：校准/建库/记录/曲线等，固件走 `ProtocolHandler.cpp` → `NetManager`

### bleClient.js 当前状态
- v2.2.0，功能正确（配网 + IP通知）
- **不需要 writeAndRead / onCalibData**（这两个放错通道了）
- 已在 bleClient.js 加的 writeAndRead/onCalibData 代码需要**移除或废弃**

### 需要新建 wifiClient.js
所有数据命令封装：
- `connect(ip, port)` / `close()` / `onConnected` / `onDisconnected`
- `sendCmd(cmd, payload)` — 命令-响应，5秒超时
- `onRealtimeData(listener)` — 实时 RMS/MDF/fatigue 监听（固件推送 `type: "data"`）
- `onStatusChange(listener)` — 状态机变化通知
- `onCalibData(listener)` — 校准过程数据（`type: "calib_data"`）
- **命令集**：start / stop / start_calib / get_rest_ref / get_calib_result / save_calib / list_curves / save_curve / list_records / delete_record / query_cz / db_start / db_mark / db_save 等

### 固件命令路由（ProtocolHandler.cpp）
统一走 WiFi WebSocket：handleJsonCommand() 路由所有 JSON 命令

### 设备信息（确认）
- 设备 IP：192.168.137.234（WiFi Direct USB以太网）
- WebSocket 端口：8888
- 电脑 IP：192.168.137.1
- 日志服务器：192.168.137.1:9876
- 串口：COM4 / 115200


## 2026-05-18 16:40 - tcpClient.js 已存在，架构确认

### 现有文件
- `utils/bleClient.js` — BLE配网（已有，v2.2.0）✅
- `utils/tcpClient.js` — WiFi数据通信（已有，v1.0）✅
- **无需新建 wifiClient.js**，直接在 tcpClient.js 上扩展

### tcpClient.js v1.0 现有能力
- WebSocket connect/disconnect/自动重连
- 心跳保活 `_setupHeartbeat`
- `send(data)` — 发送JSON
- `onMessage(fn)` / `offMessage(fn)` — 多监听器消息分发
- `enableHeartbeat(bool)`
- `setCallbacks(...)`
- `isConnected()`

### tcpClient.js 待扩展（v1.1）
- `sendCmd(cmd, payload)` — 命令-响应，5秒超时，seq序列号
- `onRealtimeData(listener)` — 监听 `type:"data"` 实时数据（RMS/MDF/fatigue）
- `onCalibData(listener)` — 监听 `type:"calib_data"` 校准过程数据
- `_notifyListeners` 内部路由：按 `data.type` 分发到不同监听器

### 固件命令路由（ProtocolHandler.cpp）
所有数据命令走 WiFi WebSocket，固件响应用 `cmd` 字段

### BLE vs WiFi 职责划分（已确认）
- BLE（bleClient.js）：配网（SSID/Password）✅
- WiFi（tcpClient.js）：所有数据命令 + 实时数据流 ✅


## 2026-05-18 16:43 - tcpClient.js v1.1 扩展完成

### 完成修改
- `utils/tcpClient.js` 追加 v1.1 扩展：
  - `sendCmd(cmd, payload)` — 命令-响应，5秒超时，seq序列号匹配
  - `onRealtimeData(fn)` / `offRealtimeData(fn)` — 监听 `type:"data"` 实时数据
  - `onCalibData(fn)` / `offCalibData(fn)` — 监听 `type:"calib_data"` 校准数据
  - `_routeTypedMessage()` — 按 `data.type` 自动路由到对应监听器
- `utils/bleClient.js` 保留原状（用户暂未决定是否清理 writeAndRead/onCalibData）

### 待清理
- bleClient.js 里的 `writeAndRead` / `_pendingCmd` / `_cmdResolve` / `_cmdReject` / `_cmdTimeout` / `_CMD_TIMEOUT_MS` / `_calibDataListeners` / `onCalibData` / `offCalibData` 等 BLE 通道无效的代码（用户未决定）

### 项目路径
- 小程序根目录：`E:\Personal\sEMG_Project\mini_program`
- tcpClient.js：`E:\Personal\sEMG_Project\mini_program\utils\tcpClient.js`
- bleClient.js：`E:\Personal\sEMG_Project\mini_program\utils\bleClient.js`


## 2026-05-18 16:45 - bleClient.js 清理完成，通道职责彻底分离

### 完成修改
- `utils/bleClient.js` 已清理 BLE 通道无效代码，回归纯配网职责
- `utils/tcpClient.js` v1.1 扩展完成，提供 sendCmd + onRealtimeData + onCalibData

### 最终架构（BLE vs WiFi）
- **BLE（bleClient.js）**：scanAndConnect → sendWifiConfig → onIpReceived → 触发页面连接 WiFi
- **WiFi（tcpClient.js）**：connect(IP) → sendCmd / onRealtimeData / onCalibData → 所有数据命令

### 项目路径
- 小程序根目录：`E:\Personal\sEMG_Project\mini_program`
- tcpClient.js：`E:\Personal\sEMG_Project\mini_program\utils\tcpClient.js`
- bleClient.js：`E:\Personal\sEMG_Project\mini_program\utils\bleClient.js`
- 固件目录：`E:\Personal\sEMG_Project\firmware`

### 待完成
- 小程序 calibrate/index.js：接入 tcpClient 的 onCalibData 实时监听
- 小程序 database/index.js：接入 tcpClient 的 sendCmd/onRealtimeData 实现建库状态机
- 小程序 monitor/index.js：改用 A区 has_curve 判断算法（接入 tcpClient）
- 固件 save_curve 命令处理（A区写入 curve_coef）
- 编译小程序预览码真机验证


## 2026-05-18 16:49 - 文件改名完成，架构最终版

### 完成修改
- `utils/tcpClient.js` → `utils/wifiClient.js`
- `pages/index/` → `pages/network/`
- TabBar 改为5项：校准/建库/实时监测/数据分析/网络配置

### wifiClient.js v1.1 最终 API
```
wifiClient.connect(ip, port)
wifiClient.send(data)
wifiClient.sendCmd(cmd, payload)      // 命令-响应，5秒超时
wifiClient.onRealtimeData(fn)
wifiClient.onCalibData(fn)
wifiClient.onMessage(fn)
wifiClient.isConnected()
wifiClient.enableHeartbeat(bool)
wifiClient.disconnect()
```

### 通道职责（最终确认）
- **BLE（bleClient.js）**：scanAndConnect → sendWifiConfig → onIpReceived → 触发页面连接 WiFi
- **WiFi（wifiClient.js）**：connect(IP) → sendCmd / onRealtimeData / onCalibData → 所有数据命令

### 小程序文件结构
```
pages/calibrate/index  — 校准（已接入 wifiClient.onCalibData）
pages/database/index   — 建库（待接入 wifiClient）
pages/realtime/index   — 实时监测（已接入 wifiClient）
pages/analysis/index   — 数据分析（已接入 wifiClient）
pages/network/index    — 网络配置（上一页改名为 network）
```

### 项目路径
- 小程序根目录：`E:\Personal\sEMG_Project\mini_program`
- wifiClient.js：`E:\Personal\sEMG_Project\mini_program\utils\wifiClient.js`
- bleClient.js：`E:\Personal\sEMG_Project\mini_program\utils\bleClient.js`
- 固件目录：`E:\Personal\sEMG_Project\firmware`

### 待完成
- database/index.js：接入 wifiClient 的 sendCmd/onRealtimeData 实现建库状态机
- calibrate/index.js：接入 wifiClient 的 onCalibData 实时监听
- monitor/index.js（现 realtime/index.js）：改用 A区 has_curve 判断算法
- 固件 save_curve 命令处理（A区写入 curve_coef）
- 编译小程序预览码真机验证


## 2026-05-18 16:50 - 待完成任务清单

| # | 任务 | 优先级 |
|---|------|--------|
| 1 | calibrate/index.js — 接入 wifiClient.onCalibData 实时监听 | 高 |
| 2 | database/index.js — 接入 wifiClient.sendCmd 建库状态机 | 高 |
| 3 | realtime/index.js — 改用 A区 has_curve 判断算法 | 中 |
| 4 | 固件 save_curve 命令 — A区写入 curve_coef | 中 |
| 5 | 固件 A区 V1.2 — 新增 has_curve/curve_coef[5]/curve_baseline 字段 | 中 |
| 6 | 编译小程序预览码真机验证 | 低 |

用户下次选择从 calibrate(index.js) 或 database(index.js) 开始。


## 2026-05-18 16:55 - calibrate/index.js 迁移完成

### calibrate/index.js 改动
- bleClient → wifiClient（所有数据命令走 WiFi）
- wifiClient.sendCmd 替代 writeAndRead
- wifiClient.onCalibData 实时数据监听
- wxml 新增实时 rms/mdf 显示（liveRestRms/liveRestMdf/liveMaxRms/liveMaxMdf）
- 固件需实现 start_calib 命令（phase:"REST"/"MAX", slot）


## 2026-05-18 17:00 - 校准流程关键修复

### 固件 start_calib 行为（已确认）
固件 `CMD_START_CALIB` 触发后：REST(10s) → **自动衔接** MAX(15s) → 完成
**没有等待按钮机制**，MAX 是固件自动切换的。

### calibrate/index.js 最终改动
- bleClient → wifiClient（全量迁移）
- `startCalibration` 改为单次 `start_calib`，REST倒计时完成后自动进入MAX倒计时
- 新增 `_onRestComplete()` 检测 `calib_data` phase 切换（REST→MAX），自动切UI状态
- 新增 `_runMaxCountdown()` 独立MAX倒计时函数
- 删除了 MAX_READY 阶段和 `onStartMax` 按钮（固件不需要）
- wxml 同步移除 MAX_READY 阶段和 onStartMax 按钮


## 2026-05-18 17:04 - 补充

### 校准设计意图（已确认，不可改）
- MAX 阶段必须有用户点击按钮的等待期（用户在 REST→MAX 之间准备时间）
- `onStartMax` + MAX_READY 状态保留，不能改为自动衔接
- 固件 `start_calib {phase:'MAX'}` 需要小程序主动发，固件不会自动切


## 2026-05-18 17:11 - Compaction 前最后状态

### 校准流程最终设计（已确认）
- REST(10s) → MAX_WAIT(用户点击"开始MAX收缩") → MAX(15s) → 完成
- 固件 `start_calib {phase:'MAX'}` 需要小程序主动发，固件不会自动切
- MAX 前必须有用户等待期（用户在 REST→MAX 之间准备时间）

### 正在进行（被打断）
- calibrate/index.js：迁移 bleClient → wifiClient
- 发现 `CMD_START_CALIB` 路由不在 `handleJsonCommand()` 中
- 需要在固件 ProtocolHandler.cpp 中加 `start_calib` JSON 命令路由

### 待继续
1. ProtocolHandler.cpp 加 `start_calib` 命令路由 → `gAppController.onCommandReceived(CMD_START_CALIB)`
2. calibrate/index.js 完成 bleClient→wifiClient 迁移
3. 数据库（database/index.js）接入 wifiClient
4. 固件 save_curve 命令 + A区 V1.2 has_curve 字段
5. realtime/index.js 改用 A区 has_curve 算法
6. 小程序预览码真机验证

### 设备信息（确认）
- 设备 IP：192.168.137.234
- WebSocket 端口：8888
- 串口：COM4 / 115200


## 2026-05-18 17:20 - 固件个性化曲线算法实现完成

### 完成改动（4文件，编译通过 ✅）
- SignalProcessor.h — setCalibration() 新增 hasCurve/curveCoef 参数，m_hasPersonalCurve + m_curveCoef[5] 成员
- SignalProcessor.cpp — setCalibration() 存储曲线系数，clearCalibration() 清空，updateFatigue() 双算法分支
- ProtocolHandler.cpp — save_calib + save_curve 均同步 SignalProcessor（含 has_curve 响应）
- AppController.cpp — init() 开机时从 A区 加载校准数据到 SignalProcessor（v3.9.12）

### 个性化算法逻辑（updateFatigue）
- m_hasPersonalCurve=true：基于个人 MDF 基准锚点(curveCoef[1])归一化计算疲劳
- m_hasPersonalCurve=false：默认 MDF 相对收缩起始值下降率算法

### 编译结果
- RAM: 69.9% (22904/32768)
- Flash: 59.2% (155176/262144)

### 待继续
- [ ] 小程序 calibrate/index.js — 完成 bleClient→wifiClient 迁移（固件端已就绪）
- [ ] 小程序 database/index.js — 接入 wifiClient 建库状态机
- [ ] 小程序 realtime/index.js — 改用 A区 has_curve 算法
- [ ] 小程序 preview 编译真机验证


## 2026-05-18 17:24 - 小程序 wifiClient 统一迁移完成 ✅

### 本轮修复（4个文件，编译通过 ✅）
- app.js — 加 `app.tcp = wifiClient` 向后兼容别名
- realtime/index.js — 迁移 `app.tcp` → `require wifiClient`，`onMessage` → `onRealtimeData`，`send()` → `sendCmd`，加 has_curve 算法标识
- analysis/index.js — 同上，`onMessage`+`send()` → `sendCmd`
- network/index.js — `app.tcp` → `require wifiClient`
- wifiClient.js — 修复重复声明（`_notifyListeners`+`_cleanup` 改赋值覆盖）

### 小程序各页面引用状态（全部已修复）
| 页面 | 引用方式 |
|------|---------|
| calibrate/index.js | ✅ `require('../../utils/wifiClient')` |
| database/index.js | ✅ `require('../../utils/wifiClient')` |
| realtime/index.js | ✅ `require('../../utils/wifiClient')` |
| analysis/index.js | ✅ `require('../../utils/wifiClient')` |
| network/index.js | ✅ `require('../../utils/wifiClient')` |
| app.js | ✅ `app.tcp = wifiClient`（向后兼容别名）|

### 已完成（全部通过）
- [x] 固件个性化曲线算法（SignalProcessor + ProtocolHandler + AppController）
- [x] 小程序 wifiClient 统一迁移
- [x] 小程序 preview 编译 ✅

### 待继续
- [ ] calibrate/index.js — 真机校准测试（固件端已就绪，WiFi命令可发送）
- [ ] realtime/index.js — 真机 has_curve 标识显示测试
- [ ] database/index.js — 真机建库流程测试


## 2026-05-18 17:37 - Session compaction

### 本次会话整体完成情况

**固件**：
- 个性化曲线算法（SignalProcessor + ProtocolHandler + AppController）
- 编译通过：RAM 69.9%, Flash 59.2%

**小程序**：
- app.js → 加 `app.tcp` 向后兼容别名
- realtime/index.js → 迁移 wifiClient API，onRealtimeData + sendCmd，加 has_curve 算法标识
- analysis/index.js → 迁移 wifiClient API，onMessage+send → sendCmd
- network/index.js → 迁移 wifiClient API
- wifiClient.js → 修复重复声明 bug（_notifyListeners + _cleanup 改赋值覆盖）
- preview 编译成功 ✅

**待完成（真机测试）**：
- calibrate/index.js 真机校准流程
- realtime/index.js has_curve 标识显示
- database/index.js 建库流程

## 2026-05-18 17:43 - 文件清理完成

### 已删除无用文件
- `mini_program/utils/const.js` — 无任何文件引用，协议常量均硬编码在页面中
- `docs/esp32-s3_datasheet_en.pdf` — 项目已迁移到 UNO R4 WiFi（非 ESP32-S3），文档过时

### 确认保留的文件（均在使用中）
- `utils/bleClient.js` — BLE配网页面使用
- `utils/wifiClient.js` — 5个TabBar页面数据命令
- `utils/logger.js` — 3个页面日志转发
- 其他文档均有效

### 编译验证
- 小程序 preview 编译通过 ✅（无多余文件干扰）


## 2026-05-18 17:55 - 小程序5页面代码审查完成

### 页面评级
- realtime (实时监测) — ✅ 最完善，功能完整
- network (网络配置) — ✅ 完整，BLE配网职责清晰
- calibrate (校准页) — ✅ 基本完整，待真机测试
- database (建库页) — ⚠️ UI完整，逻辑有缺失
- analysis (数据分析) — ⚠️ UI完整，查询命令语义存疑

### 核心缺失项（代码级）
1. database：onRealtimeData未接入，5分钟采集无实时RMS/MDF反馈
2. database：wxml 'saving'阶段缺slot展示
3. analysis：query_cz语义不清（查C区原始记录 vs MDF曲线需确认固件协议）
4. calibrate：phase='saved'无对应wxml视图
5. realtime：has_curve仅首次进入查一次，切换用户后不刷新

### 小程序路径
E:\Personal\sEMG_Project\mini_program


## 2026-05-18 18:01 - 小程序5项修复完成 + 标题/TabBar名称统一

### 本轮修改（6项）

#### 1. TabBar名称（app.json）
- "校准" → "肌电校准"
- "建库" → "数据库"
- 实时监测/数据分析/网络配置 保持不变

#### 2. 全局标题统一（app.json）
- navigationBarTitleText: "sEMG肌肉疲劳监测系统"
- app.json + 5个页面 index.json 均已更新

#### 3. calibrate/index.wxml — phase='saved'视图（核心缺失项）
- 新增 saved 阶段视图：槽位信息 + 校准数据摘要 + 拟合按钮 + 跳过按钮
- fit_done 视图保持不变

#### 4. database/index.js — 实时RMS/MDF监听（核心缺失项）
- `_startRealtimeListener()` / `_stopRealtimeListener()`
- onRealtimeData 中实时更新 liveRms/liveMdf/liveFatigue
- 采集倒计时期间实时反馈

#### 5. database/index.wxml — saving阶段显示槽位（核心缺失项）
- saving/saved 阶段展示 subject.slot 信息

#### 6. realtime/index.js — 每次onShow重查算法（核心缺失项）
- `_algorithmChecked` 改在 `onShow` 中重置，每次切回页面重新查询
- wxml 顶部新增 algorithm-tag 组件显示算法标识

### 最终状态
- 5个页面全部统一标题："sEMG肌肉疲劳监测系统"
- TabBar：肌电校准 / 数据库 / 实时监测 / 数据分析 / 网络配置
- 5项核心缺失项全部修复
- JS语法检查全部通过 ✅


## 2026-05-18 18:01 - 小程序5项修复完成 + 标题/TabBar名称统一

### 本轮修改（6项）

#### 1. TabBar名称（app.json）
- "校准" → "肌电校准"
- "建库" → "数据库"
- 实时监测/数据分析/网络配置 保持不变

#### 2. 全局标题统一（app.json）
- navigationBarTitleText: "sEMG肌肉疲劳监测系统"
- app.json + 5个页面 index.json 均已更新

#### 3. calibrate/index.wxml — phase='saved'视图（核心缺失项）
- 新增 saved 阶段视图：槽位信息 + 校准数据摘要 + 拟合按钮 + 跳过按钮
- fit_done 视图保持不变

#### 4. database/index.js — 实时RMS/MDF监听（核心缺失项）
- `_startRealtimeListener()` / `_stopRealtimeListener()`
- onRealtimeData 中实时更新 liveRms/liveMdf/liveFatigue
- 采集倒计时期间实时反馈

#### 5. database/index.wxml — saving阶段显示槽位（核心缺失项）
- saving/saved 阶段展示 subject.slot 信息

#### 6. realtime/index.js — 每次onShow重查算法（核心缺失项）
- `_algorithmChecked` 改在 `onShow` 中重置，每次切回页面重新查询
- wxml 顶部新增 algorithm-tag 组件显示算法标识

### 最终状态
- 5个页面全部统一标题："sEMG肌肉疲劳监测系统"
- TabBar：肌电校准 / 数据库 / 实时监测 / 数据分析 / 网络配置
- 5项核心缺失项全部修复
- JS语法检查全部通过 ✅

## 2026-05-18 18:04 - 代码全面审查修复（6处）

### 本轮修复
1. **calibrate/index.wxml** — result-grid MDF下降行用了 `colspan`（WXML不支持），改为4个独立cell
2. **database/index.js** — `MARK_LABELS` 是JS模块级常量，WXML无法访问，移到 `data.MARK_LABELS`
3. **calibrate/index.js** — onHide时自动重置校准（用户误切Tab丢失进度），注释掉onHide重置逻辑
4. **calibrate/index.js** — onConfirmResult后立即弹曲线询问框打断SAVED视图渲染，改为`wx.nextTick(()=>_askFitCurve())`
5. **realtime/index.wxss** — 新增algorithm-tag/algo-label/algo-value样式缺失，补全深色主题配色
6. **calibrate/index.js** — fitAsked初始值显式设为false

### 语法验证
- ✅ calibrate/index.js
- ✅ database/index.js
- ✅ realtime/index.js
- ✅ app.json


## 2026-05-18 18:07 - 小程序无用文件审查

### 确认可删除（22个文件 + 4个空目录）

| 路径 | 文件数 | 原因 |
|------|--------|------|
| `custom-tab-bar/` | 4 | app.json 用内置TabBar，无 `tabBar.component: true`，目录从未启用 |
| `components/custom-tab-bar/` | 4 | 同上副本，未使用 |
| `components/navigation-bar/` | 4 | 自定义导航栏，app.json用标准navigationBar，无引用 |
| `store/globalData.js` | 1 | 单例文件，无任何文件引用 |
| `assets/icons/` | 9 PNG | app.json TabBar无iconPath/selectedIconPath，这些图标从未被加载过 |

### 保留
- `.eslintrc.js` — 留着无害
- `components/` 空目录 — 可保留或手动删

### 项目路径
E:\Personal\sEMG_Project\mini_program

## 2026-05-18 16:20 - Phase 1 小程序代码写入完成

### 已完成文件
- [x] app.json → 5 TabBar（校准/建库/实时监测/数据分析/网络配置）
- [x] utils/storage.js → current_user工具（getCurrentUser/setCurrentUser/getSlotProfile/saveSlotProfile/getAgeGroup）
- [x] pages/calibrate/index.js → 重构（~300行，完整校准+曲线拟合）
- [x] pages/calibrate/index.wxml → 模板（profile卡片+4个phase+弹窗）
- [x] pages/calibrate/index.wxss → 精简样式
- [x] pages/calibrate/form.json → navigationBarTitleText="肌电校准"
- [x] pages/database/index.js → 新建（建库流程+记录列表+曲线库）
- [x] pages/database/index.wxml → 模板（菜单+表单+采集+标记+弹窗）
- [x] pages/database/index.wxss → 样式
- [x] pages/database/form.json → navigationBarTitleText="建库"

### 待完成
- [ ] 固件A区V1.2（StorageManager/CalibData结构体+has_curve/curve_coef字段）
- [ ] 固件：save_curve命令处理
- [ ] 小程序：bleClient.js加onCalibData/offCalibData/list_curves/delete_record/db_*命令
- [ ] 小程序：实时监测/数据分析页需改用A区has_curve判断算法
- [ ] 编译小程序预览码验证

### 关键实现细节
- 校准页 BLE命令：get_rest_ref → get_calib_result → save_calib → list_curves → save_curve
- 建库页 BLE命令：db_start → db_mark → db_save → list_records → delete_record → list_curves
- storage.js：current_user存wxStorage，槽位profile存slot0_profile/slot1_profile
- 曲线自动匹配：getAgeGroup(age)→0(<18)/1(18-35)/2(36-55)/3(56+)



## 2026-05-18 16:29 - Phase 1 小程序代码写入完成

### 已完成文件（10个）
- [x] app.json → 5 TabBar（校准/建库/实时监测/数据分析/网络配置）
- [x] utils/storage.js → current_user工具（getCurrentUser/setCurrentUser/getSlotProfile/saveSlotProfile/getAgeGroup）
- [x] pages/calibrate/index.js → 重构（~300行，完整校准+曲线拟合）
- [x] pages/calibrate/index.wxml → 模板（profile卡片+4个phase+弹窗）
- [x] pages/calibrate/index.wxss → 精简样式
- [x] pages/calibrate/form.json → navigationBarTitleText="肌电校准"
- [x] pages/database/index.js → 新建（建库流程+记录列表+曲线库）
- [x] pages/database/index.wxml → 模板（菜单+表单+采集+标记+弹窗）
- [x] pages/database/index.wxss → 样式
- [x] pages/database/form.json → navigationBarTitleText="建库"

### 待完成
- [ ] 固件A区V1.2（StorageManager/CalibData结构体+has_curve/curve_coef字段）
- [ ] 固件：save_curve命令处理
- [ ] 小程序bleClient.js：补onCalibData/offCalibData + list_curves/save_curve/delete_record + db_*命令
- [ ] 小程序：实时监测/数据分析页需改用A区has_curve判断算法
- [ ] 编译小程序预览码验证

### 核心设计
- 校准页BLE命令：get_rest_ref → get_calib_result → save_calib → list_curves → save_curve
- 建库页BLE命令：db_start → db_mark → db_save → list_records → delete_record → list_curves
- storage.js：current_user存wxStorage，槽位profile存slot0_profile/slot1_profile
- 曲线自动匹配：getAgeGroup(age)→0(<18)/1(18-35)/2(36-55)/3(56+)，gender+age_group+handedness精确匹配
- A区V1.2：2槽位×校准数据，has_curve(bool)+curve_coef[5]+curve_baseline_rms/mdf
- B区曲线库：curve_id 1-16（2性别×4年龄段×2利手），status=1可用


## 2026-05-18 16:36 - Phase 2 固件分析完成

### 固件命令处理架构（ProtocolHandler.cpp）

固件命令统一在 `ProtocolHandler::handleJsonCommand()` 路由：
- `query_cz` → `gAppController.handleQueryCZ()`
- `save_record` → `gAppController.handleSaveRecord()`
- `list_records` → `gAppController.handleListRecords()`
- `delete_record` → `gAppController.handleDeleteRecord()`
- `list_curves` → `gAppController.handleListCurves()`
- `gen_personal_curve` → `gAppController.handleGenPersonalCurve()`
- `get_curve` → `gAppController.handleGetCurve()`
- `start_db_feature` → `gAppController.handleStartDbFeature()`
- `capture_raw_phase` → `gAppController.handleCaptureRawPhase()`
- `raw_phase_done` → `gAppController.handleRawPhaseDone()`
- `save_calib` → `gStorage.UpdatePersonalCalib()` + `gSignal.setCalibration()`
- `start_calib` → `gAppController.onCommandReceived(CMD_START_CALIB)`
- `start` → IDLE时启动校准，MONITORING时发确认
- `stop` → `gAppController.onCommandReceived(CMD_STOP)`

固件响应用 `cmd` 字段（非 `type`），小程序需要适配。

固件 AZone 结构体已有 curve 字段（PersonalCalibData_t）：
- `has_curve` (uint8_t)
- `curve_coef[5]` (float[5])
- `curve_baseline_rms` / `curve_baseline_mdf` (float)

### 小程序 bleClient.js 改造（v2.3.0 新增）

**writeAndRead 命令-响应机制**（核心，8秒超时）：
- 发送 `{cmd, seq, payload}` → 等待响应 `{cmd:"ack", seq}` → resolve
- 内部 `_cmdPendingResolves[seq]` / `_cmdPendingRejects[seq]`
- 接收 JSON 响应时匹配 seq，自动 resolve/reject

**onCalibData 监听器**（用于校准页实时数据）：
- 监听 `type='calib_data'` 的 BLE notify
- 内部 `_calibDataListeners[]`

**扩展 _tryParseCompleteJson**：
- `data.cmd === 'ack'` && seq匹配 → resolve pending promise
- `data.type === 'calib_data'` → 通知 `_calibDataListeners`
- 已有 `cmd === 'status'` 解析

### 待完成

- [ ] 固件：save_curve 命令处理（A区写入 curve_coef）
- [ ] 小程序：bleClient.js 补全 `get_rest_ref`/`get_calib_result`/`db_start`/`db_mark`/`db_save` 命令封装
- [ ] 小程序：calibrate/index.js 接入 `onCalibData` 实时监听
- [ ] 小程序：database/index.js 实现建库状态机（调用 db_* 命令）
- [ ] 小程序：monitor/index.js 改用 A区 has_curve 判断算法
- [ ] 编译小程序预览码验证

### 设备信息（本次会话）
- 设备 IP：192.168.137.234（WiFi Direct USB以太网）
- WebSocket 端口：8888
- 电脑 IP：192.168.137.1
- 日志服务器：192.168.137.1:9876
- 串口：COM4 / 115200


## 2026-05-18 16:38 - ⚠️ 关键架构纠正：BLE vs WiFi 通道分离

### 正确的通信架构
- **BLE（bleClient.js）**→ 仅用于配网：`sendWifiConfig(ssid, password)` + `onIpReceived(callback)`，固件走 `BleConfigServer.cpp`
- **WiFi WebSocket（wifiClient.js）**→ 所有数据命令走这里：校准/建库/记录/曲线等，固件走 `ProtocolHandler.cpp` → `NetManager`

### bleClient.js 当前状态
- v2.2.0，功能正确（配网 + IP通知）
- **不需要 writeAndRead / onCalibData**（这两个放错通道了）
- 已在 bleClient.js 加的 writeAndRead/onCalibData 代码需要**移除或废弃**

### 需要新建 wifiClient.js
所有数据命令封装：
- `connect(ip, port)` / `close()` / `onConnected` / `onDisconnected`
- `sendCmd(cmd, payload)` — 命令-响应，5秒超时
- `onRealtimeData(listener)` — 实时 RMS/MDF/fatigue 监听（固件推送 `type: "data"`）
- `onStatusChange(listener)` — 状态机变化通知
- `onCalibData(listener)` — 校准过程数据（`type: "calib_data"`）
- **命令集**：start / stop / start_calib / get_rest_ref / get_calib_result / save_calib / list_curves / save_curve / list_records / delete_record / query_cz / db_start / db_mark / db_save 等

### 固件命令路由（ProtocolHandler.cpp）
统一走 WiFi WebSocket：handleJsonCommand() 路由所有 JSON 命令

### 设备信息（确认）
- 设备 IP：192.168.137.234（WiFi Direct USB以太网）
- WebSocket 端口：8888
- 电脑 IP：192.168.137.1
- 日志服务器：192.168.137.1:9876
- 串口：COM4 / 115200


## 2026-05-18 16:40 - tcpClient.js 已存在，架构确认

### 现有文件
- `utils/bleClient.js` — BLE配网（已有，v2.2.0）✅
- `utils/tcpClient.js` — WiFi数据通信（已有，v1.0）✅
- **无需新建 wifiClient.js**，直接在 tcpClient.js 上扩展

### tcpClient.js v1.0 现有能力
- WebSocket connect/disconnect/自动重连
- 心跳保活 `_setupHeartbeat`
- `send(data)` — 发送JSON
- `onMessage(fn)` / `offMessage(fn)` — 多监听器消息分发
- `enableHeartbeat(bool)`
- `setCallbacks(...)`
- `isConnected()`

### tcpClient.js 待扩展（v1.1）
- `sendCmd(cmd, payload)` — 命令-响应，5秒超时，seq序列号
- `onRealtimeData(listener)` — 监听 `type:"data"` 实时数据（RMS/MDF/fatigue）
- `onCalibData(listener)` — 监听 `type:"calib_data"` 校准过程数据
- `_notifyListeners` 内部路由：按 `data.type` 分发到不同监听器

### 固件命令路由（ProtocolHandler.cpp）
所有数据命令走 WiFi WebSocket，固件响应用 `cmd` 字段

### BLE vs WiFi 职责划分（已确认）
- BLE（bleClient.js）：配网（SSID/Password）✅
- WiFi（tcpClient.js）：所有数据命令 + 实时数据流 ✅


## 2026-05-18 16:43 - tcpClient.js v1.1 扩展完成

### 完成修改
- `utils/tcpClient.js` 追加 v1.1 扩展：
  - `sendCmd(cmd, payload)` — 命令-响应，5秒超时，seq序列号匹配
  - `onRealtimeData(fn)` / `offRealtimeData(fn)` — 监听 `type:"data"` 实时数据
  - `onCalibData(fn)` / `offCalibData(fn)` — 监听 `type:"calib_data"` 校准数据
  - `_routeTypedMessage()` — 按 `data.type` 自动路由到对应监听器
- `utils/bleClient.js` 保留原状（用户暂未决定是否清理 writeAndRead/onCalibData）

### 待清理
- bleClient.js 里的 `writeAndRead` / `_pendingCmd` / `_cmdResolve` / `_cmdReject` / `_cmdTimeout` / `_CMD_TIMEOUT_MS` / `_calibDataListeners` / `onCalibData` / `offCalibData` 等 BLE 通道无效的代码（用户未决定）

### 项目路径
- 小程序根目录：`E:\Personal\sEMG_Project\mini_program`
- tcpClient.js：`E:\Personal\sEMG_Project\mini_program\utils\tcpClient.js`
- bleClient.js：`E:\Personal\sEMG_Project\mini_program\utils\bleClient.js`


## 2026-05-18 16:45 - bleClient.js 清理完成，通道职责彻底分离

### 完成修改
- `utils/bleClient.js` 已清理 BLE 通道无效代码，回归纯配网职责
- `utils/tcpClient.js` v1.1 扩展完成，提供 sendCmd + onRealtimeData + onCalibData

### 最终架构（BLE vs WiFi）
- **BLE（bleClient.js）**：scanAndConnect → sendWifiConfig → onIpReceived → 触发页面连接 WiFi
- **WiFi（tcpClient.js）**：connect(IP) → sendCmd / onRealtimeData / onCalibData → 所有数据命令

### 项目路径
- 小程序根目录：`E:\Personal\sEMG_Project\mini_program`
- tcpClient.js：`E:\Personal\sEMG_Project\mini_program\utils\tcpClient.js`
- bleClient.js：`E:\Personal\sEMG_Project\mini_program\utils\bleClient.js`
- 固件目录：`E:\Personal\sEMG_Project\firmware`

### 待完成
- 小程序 calibrate/index.js：接入 tcpClient 的 onCalibData 实时监听
- 小程序 database/index.js：接入 tcpClient 的 sendCmd/onRealtimeData 实现建库状态机
- 小程序 monitor/index.js：改用 A区 has_curve 判断算法（接入 tcpClient）
- 固件 save_curve 命令处理（A区写入 curve_coef）
- 编译小程序预览码真机验证


## 2026-05-18 16:49 - 文件改名完成，架构最终版

### 完成修改
- `utils/tcpClient.js` → `utils/wifiClient.js`
- `pages/index/` → `pages/network/`
- TabBar 改为5项：校准/建库/实时监测/数据分析/网络配置

### wifiClient.js v1.1 最终 API
```
wifiClient.connect(ip, port)
wifiClient.send(data)
wifiClient.sendCmd(cmd, payload)      // 命令-响应，5秒超时
wifiClient.onRealtimeData(fn)
wifiClient.onCalibData(fn)
wifiClient.onMessage(fn)
wifiClient.isConnected()
wifiClient.enableHeartbeat(bool)
wifiClient.disconnect()
```

### 通道职责（最终确认）
- **BLE（bleClient.js）**：scanAndConnect → sendWifiConfig → onIpReceived → 触发页面连接 WiFi
- **WiFi（wifiClient.js）**：connect(IP) → sendCmd / onRealtimeData / onCalibData → 所有数据命令

### 小程序文件结构
```
pages/calibrate/index  — 校准（已接入 wifiClient.onCalibData）
pages/database/index   — 建库（待接入 wifiClient）
pages/realtime/index   — 实时监测（已接入 wifiClient）
pages/analysis/index   — 数据分析（已接入 wifiClient）
pages/network/index    — 网络配置（上一页改名为 network）
```

### 项目路径
- 小程序根目录：`E:\Personal\sEMG_Project\mini_program`
- wifiClient.js：`E:\Personal\sEMG_Project\mini_program\utils\wifiClient.js`
- bleClient.js：`E:\Personal\sEMG_Project\mini_program\utils\bleClient.js`
- 固件目录：`E:\Personal\sEMG_Project\firmware`

### 待完成
- database/index.js：接入 wifiClient 的 sendCmd/onRealtimeData 实现建库状态机
- calibrate/index.js：接入 wifiClient 的 onCalibData 实时监听
- monitor/index.js（现 realtime/index.js）：改用 A区 has_curve 判断算法
- 固件 save_curve 命令处理（A区写入 curve_coef）
- 编译小程序预览码真机验证


## 2026-05-18 16:50 - 待完成任务清单

| # | 任务 | 优先级 |
|---|------|--------|
| 1 | calibrate/index.js — 接入 wifiClient.onCalibData 实时监听 | 高 |
| 2 | database/index.js — 接入 wifiClient.sendCmd 建库状态机 | 高 |
| 3 | realtime/index.js — 改用 A区 has_curve 判断算法 | 中 |
| 4 | 固件 save_curve 命令 — A区写入 curve_coef | 中 |
| 5 | 固件 A区 V1.2 — 新增 has_curve/curve_coef[5]/curve_baseline 字段 | 中 |
| 6 | 编译小程序预览码真机验证 | 低 |

用户下次选择从 calibrate(index.js) 或 database(index.js) 开始。


## 2026-05-18 16:55 - calibrate/index.js 迁移完成

### calibrate/index.js 改动
- bleClient → wifiClient（所有数据命令走 WiFi）
- wifiClient.sendCmd 替代 writeAndRead
- wifiClient.onCalibData 实时数据监听
- wxml 新增实时 rms/mdf 显示（liveRestRms/liveRestMdf/liveMaxRms/liveMaxMdf）
- 固件需实现 start_calib 命令（phase:"REST"/"MAX", slot）


## 2026-05-18 17:00 - 校准流程关键修复

### 固件 start_calib 行为（已确认）
固件 `CMD_START_CALIB` 触发后：REST(10s) → **自动衔接** MAX(15s) → 完成
**没有等待按钮机制**，MAX 是固件自动切换的。

### calibrate/index.js 最终改动
- bleClient → wifiClient（全量迁移）
- `startCalibration` 改为单次 `start_calib`，REST倒计时完成后自动进入MAX倒计时
- 新增 `_onRestComplete()` 检测 `calib_data` phase 切换（REST→MAX），自动切UI状态
- 新增 `_runMaxCountdown()` 独立MAX倒计时函数
- 删除了 MAX_READY 阶段和 `onStartMax` 按钮（固件不需要）
- wxml 同步移除 MAX_READY 阶段和 onStartMax 按钮


## 2026-05-18 17:04 - 补充

### 校准设计意图（已确认，不可改）
- MAX 阶段必须有用户点击按钮的等待期（用户在 REST→MAX 之间准备时间）
- `onStartMax` + MAX_READY 状态保留，不能改为自动衔接
- 固件 `start_calib {phase:'MAX'}` 需要小程序主动发，固件不会自动切


## 2026-05-18 17:11 - Compaction 前最后状态

### 校准流程最终设计（已确认）
- REST(10s) → MAX_WAIT(用户点击"开始MAX收缩") → MAX(15s) → 完成
- 固件 `start_calib {phase:'MAX'}` 需要小程序主动发，固件不会自动切
- MAX 前必须有用户等待期（用户在 REST→MAX 之间准备时间）

### 正在进行（被打断）
- calibrate/index.js：迁移 bleClient → wifiClient
- 发现 `CMD_START_CALIB` 路由不在 `handleJsonCommand()` 中
- 需要在固件 ProtocolHandler.cpp 中加 `start_calib` JSON 命令路由

### 待继续
1. ProtocolHandler.cpp 加 `start_calib` 命令路由 → `gAppController.onCommandReceived(CMD_START_CALIB)`
2. calibrate/index.js 完成 bleClient→wifiClient 迁移
3. 数据库（database/index.js）接入 wifiClient
4. 固件 save_curve 命令 + A区 V1.2 has_curve 字段
5. realtime/index.js 改用 A区 has_curve 算法
6. 小程序预览码真机验证

### 设备信息（确认）
- 设备 IP：192.168.137.234
- WebSocket 端口：8888
- 串口：COM4 / 115200


## 2026-05-18 17:20 - 固件个性化曲线算法实现完成

### 完成改动（4文件，编译通过 ✅）
- SignalProcessor.h — setCalibration() 新增 hasCurve/curveCoef 参数，m_hasPersonalCurve + m_curveCoef[5] 成员
- SignalProcessor.cpp — setCalibration() 存储曲线系数，clearCalibration() 清空，updateFatigue() 双算法分支
- ProtocolHandler.cpp — save_calib + save_curve 均同步 SignalProcessor（含 has_curve 响应）
- AppController.cpp — init() 开机时从 A区 加载校准数据到 SignalProcessor（v3.9.12）

### 个性化算法逻辑（updateFatigue）
- m_hasPersonalCurve=true：基于个人 MDF 基准锚点(curveCoef[1])归一化计算疲劳
- m_hasPersonalCurve=false：默认 MDF 相对收缩起始值下降率算法

### 编译结果
- RAM: 69.9% (22904/32768)
- Flash: 59.2% (155176/262144)

### 待继续
- [ ] 小程序 calibrate/index.js — 完成 bleClient→wifiClient 迁移（固件端已就绪）
- [ ] 小程序 database/index.js — 接入 wifiClient 建库状态机
- [ ] 小程序 realtime/index.js — 改用 A区 has_curve 算法
- [ ] 小程序 preview 编译真机验证


## 2026-05-18 17:24 - 小程序 wifiClient 统一迁移完成 ✅

### 本轮修复（4个文件，编译通过 ✅）
- app.js — 加 `app.tcp = wifiClient` 向后兼容别名
- realtime/index.js — 迁移 `app.tcp` → `require wifiClient`，`onMessage` → `onRealtimeData`，`send()` → `sendCmd`，加 has_curve 算法标识
- analysis/index.js — 同上，`onMessage`+`send()` → `sendCmd`
- network/index.js — `app.tcp` → `require wifiClient`
- wifiClient.js — 修复重复声明（`_notifyListeners`+`_cleanup` 改赋值覆盖）

### 小程序各页面引用状态（全部已修复）
| 页面 | 引用方式 |
|------|---------|
| calibrate/index.js | ✅ `require('../../utils/wifiClient')` |
| database/index.js | ✅ `require('../../utils/wifiClient')` |
| realtime/index.js | ✅ `require('../../utils/wifiClient')` |
| analysis/index.js | ✅ `require('../../utils/wifiClient')` |
| network/index.js | ✅ `require('../../utils/wifiClient')` |
| app.js | ✅ `app.tcp = wifiClient`（向后兼容别名）|

### 已完成（全部通过）
- [x] 固件个性化曲线算法（SignalProcessor + ProtocolHandler + AppController）
- [x] 小程序 wifiClient 统一迁移
- [x] 小程序 preview 编译 ✅

### 待继续
- [ ] calibrate/index.js — 真机校准测试（固件端已就绪，WiFi命令可发送）
- [ ] realtime/index.js — 真机 has_curve 标识显示测试
- [ ] database/index.js — 真机建库流程测试


## 2026-05-18 17:37 - Session compaction

### 本次会话整体完成情况

**固件**：
- 个性化曲线算法（SignalProcessor + ProtocolHandler + AppController）
- 编译通过：RAM 69.9%, Flash 59.2%

**小程序**：
- app.js → 加 `app.tcp` 向后兼容别名
- realtime/index.js → 迁移 wifiClient API，onRealtimeData + sendCmd，加 has_curve 算法标识
- analysis/index.js → 迁移 wifiClient API，onMessage+send → sendCmd
- network/index.js → 迁移 wifiClient API
- wifiClient.js → 修复重复声明 bug（_notifyListeners + _cleanup 改赋值覆盖）
- preview 编译成功 ✅

**待完成（真机测试）**：
- calibrate/index.js 真机校准流程
- realtime/index.js has_curve 标识显示
- database/index.js 建库流程

## 2026-05-18 17:43 - 文件清理完成

### 已删除无用文件
- `mini_program/utils/const.js` — 无任何文件引用，协议常量均硬编码在页面中
- `docs/esp32-s3_datasheet_en.pdf` — 项目已迁移到 UNO R4 WiFi（非 ESP32-S3），文档过时

### 确认保留的文件（均在使用中）
- `utils/bleClient.js` — BLE配网页面使用
- `utils/wifiClient.js` — 5个TabBar页面数据命令
- `utils/logger.js` — 3个页面日志转发
- 其他文档均有效

### 编译验证
- 小程序 preview 编译通过 ✅（无多余文件干扰）


## 2026-05-18 17:55 - 小程序5页面代码审查完成

### 页面评级
- realtime (实时监测) — ✅ 最完善，功能完整
- network (网络配置) — ✅ 完整，BLE配网职责清晰
- calibrate (校准页) — ✅ 基本完整，待真机测试
- database (建库页) — ⚠️ UI完整，逻辑有缺失
- analysis (数据分析) — ⚠️ UI完整，查询命令语义存疑

### 核心缺失项（代码级）
1. database：onRealtimeData未接入，5分钟采集无实时RMS/MDF反馈
2. database：wxml 'saving'阶段缺slot展示
3. analysis：query_cz语义不清（查C区原始记录 vs MDF曲线需确认固件协议）
4. calibrate：phase='saved'无对应wxml视图
5. realtime：has_curve仅首次进入查一次，切换用户后不刷新

### 小程序路径
E:\Personal\sEMG_Project\mini_program


## 2026-05-18 18:01 - 小程序5项修复完成 + 标题/TabBar名称统一

### 本轮修改（6项）

#### 1. TabBar名称（app.json）
- "校准" → "肌电校准"
- "建库" → "数据库"
- 实时监测/数据分析/网络配置 保持不变

#### 2. 全局标题统一（app.json）
- navigationBarTitleText: "sEMG肌肉疲劳监测系统"
- app.json + 5个页面 index.json 均已更新

#### 3. calibrate/index.wxml — phase='saved'视图（核心缺失项）
- 新增 saved 阶段视图：槽位信息 + 校准数据摘要 + 拟合按钮 + 跳过按钮
- fit_done 视图保持不变

#### 4. database/index.js — 实时RMS/MDF监听（核心缺失项）
- `_startRealtimeListener()` / `_stopRealtimeListener()`
- onRealtimeData 中实时更新 liveRms/liveMdf/liveFatigue
- 采集倒计时期间实时反馈

#### 5. database/index.wxml — saving阶段显示槽位（核心缺失项）
- saving/saved 阶段展示 subject.slot 信息

#### 6. realtime/index.js — 每次onShow重查算法（核心缺失项）
- `_algorithmChecked` 改在 `onShow` 中重置，每次切回页面重新查询
- wxml 顶部新增 algorithm-tag 组件显示算法标识

### 最终状态
- 5个页面全部统一标题："sEMG肌肉疲劳监测系统"
- TabBar：肌电校准 / 数据库 / 实时监测 / 数据分析 / 网络配置
- 5项核心缺失项全部修复
- JS语法检查全部通过 ✅


## 2026-05-18 18:04 - 代码全面审查修复（6处）

### 本轮修复
1. **calibrate/index.wxml** — result-grid MDF下降行用了 `colspan`（WXML不支持），改为4个独立cell
2. **database/index.js** — `MARK_LABELS` 是JS模块级常量，WXML无法访问，移到 `data.MARK_LABELS`
3. **calibrate/index.js** — onHide时自动重置校准（用户误切Tab丢失进度），注释掉onHide重置逻辑
4. **calibrate/index.js** — onConfirmResult后立即弹曲线询问框打断SAVED视图渲染，改为`wx.nextTick(()=>_askFitCurve())`
5. **realtime/index.wxss** — 新增algorithm-tag/algo-label/algo-value样式缺失，补全深色主题配色
6. **calibrate/index.js** — fitAsked初始值显式设为false

### 语法验证
- ✅ calibrate/index.js
- ✅ database/index.js
- ✅ realtime/index.js
- ✅ app.json


## 2026-05-18 18:07 - 小程序无用文件审查

### 确认可删除（22个文件 + 4个空目录）

| 路径 | 文件数 | 原因 |
|------|--------|------|
| `custom-tab-bar/` | 4 | app.json 用内置TabBar，无 `tabBar.component: true`，目录从未启用 |
| `components/custom-tab-bar/` | 4 | 同上副本，未使用 |
| `components/navigation-bar/` | 4 | 自定义导航栏，app.json用标准navigationBar，无引用 |
| `store/globalData.js` | 1 | 单例文件，无任何文件引用 |
| `assets/icons/` | 9 PNG | app.json TabBar无iconPath/selectedIconPath，这些图标从未被加载过 |

### 保留
- `.eslintrc.js` — 留着无害
- `components/` 空目录 — 可保留或手动删

### 项目路径
E:\Personal\sEMG_Project\mini_program


## 2026-05-18 18:30 - 校准/数据库页面布局与备份对齐完成

### 目标
将 `calibrate` 和 `database` 两个页面的布局/样式与今早备份（`E:\Personal\sEMG_Project_Backup_20260518_120028`）中的格式对齐。

### 完成工作

#### calibrate 页面
- **WXML**：将所有阶段（idle/resting/max_ready/max_contract/result/saved/fitting/fit_done）的容器从 `phase-panel` 改为 `status-area`，并采用备份的语义化 class 名：
  - 提示区域 → `hint-box` + `hint-phase` + `hint-text`
  - 实时数据 → `live-values` + `live-item`（替换旧 `live-data-row`）
  - 结果展示 → `result-summary` + `result-item`（替换旧 `result-grid` 表格）
  - 验证结果 → `validation-result` + `val-icon` + `val-text`
  - 成功状态 → `success-box` + `success-icon/text/sub`
  - 曲线引导 → `curve-match-guide` + `curve-guide-hint/actions`
- **WXSS**：完整替换为备份的 `calibrate.wxss`（深色主题 `#0d0d0d`），新增所有上述 class 的定义。

#### database 页面
- **WXML**：全文重写，采用备份的风格模式：
  - 所有阶段用 `status-area` 包装
  - 表单用 `form-group` + `form-label` + `form-input`
  - 采集阶段用 `live-values` + `live-item` + `db-phase-badge`
  - 标记阶段用 `phase-transition` + `db-phase-grid` + `db-phase-btn`
  - 完成状态用 `success-box`
  - 弹窗用 `modal-box` + `modal-header` + `modal-actions-bottom`（替换旧 `modal-panel`）
- **WXSS**：完整写入，定义所有新 class，深色主题与 calibrate 保持一致。

### 关键设计决策
- 统一使用备份的深色主题（`#0d0d0d` 背景），不再用浅色 `#f5f5f5`
- `status-area` 作为统一的阶段内容容器
- `live-values` / `live-item` 网格展示实时数据，替代旧的行式布局
- 弹窗统一用 `modal-box`（带 `modal-header` 和关闭按钮）

### 文件变更清单
| 文件 | 操作 |
|------|--------|
| `pages/calibrate/index.wxml` | 重构（8个阶段） |
| `pages/calibrate/index.wxss` | 完整替换（备份格式） |
| `pages/database/index.wxml` | 全文重写 |
| `pages/database/index.wxss` | 全文重写 |

### 待验证
- [ ] 微信开发者工具编译是否通过（无 WXML 语法错误）
- [ ] 真机预览：校准流程各阶段 UI 显示是否正常
- [ ] 真机预览：数据库建库流程各阶段 UI 是否正常


## 2026-05-18 18:44 - 小程序离线/在线逻辑确认

### 用户问题
打开小程序，此时没有硬件连接网络，那么小程序上面每个页面应该是有一些固定内容可以显示的，不依赖于设备是否联网，设备联网仅限于实时数据更新，以及小程序与设备需要交互的部分。

### 确认的设计原则
- **离线内容（不依赖设备连接，打开就能看）**：
  - 所有页面的静态布局、标题、说明文字
  - 校准页的流程介绍
  - 数据库页的表单填写（还没开始采集时）
  - 设置页的配置项（除需要设备通信的部分）
  - 分析页的静态框架
- **需要设备连接的功能**：
  - 实时 RMS / MDF / 疲劳度 数据更新
  - 所有向设备发命令的操作（start / save / query 等）
  - 从设备 Flash 读取历史数据

### 待执行
- [ ] 审查5个页面，确保离线内容不错误依赖 `connected` 变量
- [ ] 设备交互操作加连接检查，未连接时 disable 按钮或给提示
- [ ] realtime 页：未连接时显示「请先连接设备」而非空白或 `--`
- [ ] analysis 页：未连接时隐藏图表区域并给提示

### 备注
备份路径：`E:\Personal\sEMG_Project_Backup_20260518_120028\mini_program\pages\calibrate\`


## 2026-05-18 18:51 - 小程序离线/在线内容划分（设计决策）

### 用户确认的设计原则

**离线内容（不依赖设备连接，打开就能看）**：
- 所有页面的静态布局、标题、说明文字
- 校准页的流程介绍
- 数据库页的表单填写（还没开始采集时）
- 设置页的配置项（除需要设备通信的部分）
- 分析页的静态框架

**需要设备连接的功能**：
- 实时 RMS / MDF / 疲劳度 数据更新
- 所有向设备发命令的操作（start / save / query 等）
- 从设备 Flash 读取历史数据

### 待执行检查清单
- [ ] 审查5个页面，确保离线内容不错误依赖 `connected` 变量
- [ ] 设备交互操作加连接检查，未连接时 disable 按钮或给提示
- [ ] realtime 页：未连接时显示「请先连接设备」而非空白或 `--`
- [ ] analysis 页：未连接时隐藏图表区域并给提示
- [ ] calibrate 页：未连接时 disable「开始校准」按钮并提示
- [ ] database 页：未连接时 disable「开始采集」按钮并提示
