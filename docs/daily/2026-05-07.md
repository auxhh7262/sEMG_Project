# 2026-05-07 Memory

## 项目：sEMG固件 V1.4 + 微信小程序通信

### 微信小程序代码位置
- **路径：** `E:\Personal\sEMG\code\0421`
- 用户已打开微信开发者工具（桌面应用）
- 需要实现小程序与固件的通信（WiFi连接）

### 会话要点
1. 预算紧张：今日免费积分已耗尽，预计0时更新；预算不足(insufficient)，无法进行深度搜索
2. 用户需要小程序连接sEMG设备的WiFi热点（AP模式），建立TCP通信获取实时肌电数据
3. 小程序需要使用WiFi相关API（微信小程序有`wx.connectWifi`等接口）

### 待续任务
- 读取 `E:\Personal\sEMG\code\0421` 目录结构，了解小程序现有代码
- 为小程序实现WiFi连接和TCP通信功能，与固件配对
# 2026-05-07 Memory

## 项目：sEMG固件 V1.4 + 微信小程序通信

### 微信小程序代码位置
- **路径：** `E:\Personal\sEMG\code\0421`
- 用户已打开微信开发者工具（桌面应用）
- 需要实现小程序与固件的通信（WiFi连接）

### 会话要点
1. 预算紧张：今日免费积分已耗尽，预计0时更新；预算不足(insufficient)，无法进行深度搜索
2. 用户需要小程序连接sEMG设备的WiFi热点（AP模式），建立TCP通信获取实时肌电数据
3. 小程序需要使用WiFi相关API（微信小程序有`wx.connectWifi`等接口）

### 待续任务
- 读取 `E:\Personal\sEMG\code\0421` 目录结构，了解小程序现有代码
- 为小程序实现WiFi连接和TCP通信功能，与固件配对

---

## 2026-05-07 补充（06:16 UTC）

### 小程序代码深度分析完成

**已完成阅读的文件：**
- `pages/realtime/index.js` — 示波器引擎 + TCP数据消费
- `pages/realtime/index.wxml` — Canvas + 数据面板
- `pages/calibrate/index.js` — [附件图片，未解析]
- `pages/analysis/index.js` — [附件图片，未解析]
- `app.js` — 全局错误过滤
- `utils/bleClient.js` — BLE分段写入（v2.0.0，已对齐固件UUID）
- `utils/tcpClient.js` — [未读取]
- `utils/const.js` — [附件图片，未解析]

**现有小程序已实现：**
- ✅ BLE扫描/连接/配网（index页）→ 已对齐固件 BleConfigServer.cpp UUID
- ✅ TCP连接 + 实时流消费（realtime页）→ Canvas示波器 + RMS/疲劳度数字
- ✅ 断网静默重连兜底（realtime页）

**待实现功能（优先级待用户确认）：**
1. calibrate页完善 — rest/max contraction引导流程
2. analysis页基础框架 — 图表 + 历史数据列表
3. B区数据操作 — 导出电脑/曲线拟合/回存/删除
4. 数据库采集 — 多人建库管理

**固件↔小程序协议对照（已确认）：**
- TCP推流格式：`{ "r": 0.12, "f": 45.3, "ts": 1715xxxxxxx }`
- TCP发指令：`{ "cmd": "start"|"stop"|"reset_calib"|"handshake" }`
- BLE UUID：SSID→19b10001, Password→19b10004, Notify→19b10003

**固件存储架构（已确认）：**
- A区(0x000000-0x001FFF)：2扇区×4KB个人档案，双缓冲轮转写
- B区(0x002000-0x1xxFFF)：49槽×40KB肌电特征数据库+曲线库
- C区(0x200000-0xFFFFFF)：实时监控数据，12字节/点，64KB块管理

**待用户确认开发顺序**
---

## 2026-05-07 补充（06:20 UTC+8）

### 本次会话完成工作

#### 1. 校准页（calibrate）完整实现
- **WXML** (105行)：顶部进度条 → 阶段图标+提示 → 实时RMS/MDF数值/倒计时/进度圆环 → 底部操作按钮
- **JS** (267行)：5阶段状态机（idle → resting → max_contraction → result → saved）
  - 验证逻辑与固件 `CalibrationManager.cpp` 对齐：`max_rms/rest_rms ≥ 2.0`，MDF下降 ≥ 10%
  - 采集完成后发 `save_calib` 指令写入设备A区
- **WXSS** (213行)：深色主题，与 realtime 页面配色一致

#### 2. tcpClient.js 扩展（v1.6.1 → v1.6.2）
- **问题**：原有版本只导出 `setCallbacks`，不支持多监听器 on/off 模式，calibrate 页面无法注册/注销独立监听器
- **修复**：新增 `onMessage(fn)` / `offMessage(fn)` / `close()`，向后兼容原有 API
- 改动：6484 → 6858 字节

#### 3. 开发优先级（用户待确认）
1. analysis 页（历史数据 + 图表 + 导出）
2. 实时页增强（显示 activation 收缩力度）
3. B区协议设计（小程序 ↔ 固件 导出/拟合/删除协议）

#### 4. 小程序项目路径
- **代码**：`E:\Personal\sEMG\code\0421`

#### 5. 开发日志
- `C:\Users\honghuang\.qclaw\workspace\semg-miniprogram-dev_20260507-0616.md` — 本次开发的详细记录文件
---

## 2026-05-07 补充（22:39 UTC / 06:39 北京时间）

### 实时页增强 — 收缩力度卡片（本次完成）

**需求**：实时页上半部分显示3张数据卡片（RMS / 疲劳度 / 收缩力度），下半部分示波器显示30条历史数据

**实现位置**：`E:\Personal\sEMG\code\0421\pages\realtime\`

**文件改动：**
- `index.wxml` (2.0KB)：3卡片行 + chart-header + waveCanvas + 重连badge + 控制台
- `index.js` (11.8KB)：3 Canvas + dataQueue 环形缓冲 MAX_HISTORY=30 + sparkRms/Fat/Act 迷你趋势线各20点 + 收缩力度计算逻辑
- `index.wxss` (3.0KB)：卡片行flex布局 + 疲劳态动态边框 + 迷你趋势线28px

**关键逻辑：**
- `收缩力度 = ((rms - rest_rms) / (max_rms - rest_rms)) * 100`，从 `wx.getStorageSync('calib_data')` 读取校准参数
- `triggerRecalibrate()` 清除本地 calib_data，下次进入校准页重新校准
- `_getFatColor()` 根据疲劳度自动变色（绿<50%<橙<80%<红）
- 断线重连：通过 `app.onRealtimeDisconnect` 事件通道（需配合 app.js 下一步扩展）

**待续：**
1. analysis 页（历史数据列表 + 图表 + 导出）
2. B区协议设计（小程序 ↔ 固件导出/拟合/删除协议）
3. app.js `onRealtimeDisconnect` 事件通道（让 tcpClient 的 disconnected 回调通知各页面）

### 小程序项目路径
- **代码**：`E:\Personal\sEMG\code\0421`
- **开发日志**：`C:\Users\honghuang\.qclaw\workspace\semg-miniprogram-dev_20260507-0616.md`

### 固件项目路径
- **位置**：`E:\Personal\sEMG\sEMG_Firmware_V1.4`

### 预算状态
- 积分预算不足(insufficient)，无法进行深度搜索
---

## 2026-05-07 补充（06:47 北京时间）- 会话压缩前flush

### 固件协议确认 — 5字段已完整

**固件 NetManager.cpp sendData() 已完整发送 6 个字段：**
```json
{"ts":1715xxxxxxx,"r":0.12,"m":95.3,"f":0.23,"q":85,"a":0.67}
```
- `ts` = 时间戳（sec + ms）
- `r` = RMS 原始值
- `m` = MDF 原始值 **（当前小程序未显示！）**
- `f` = 疲劳度（固件已算好，0.00-1.00）
- `q` = 信号质量
- `a` = 收缩力度（固件已算好，0.00-1.00）

**用户需求**：小程序 realtime 页面显示全部 5 个值：**时间 + RMS + MDF + 收缩力度 + 疲劳度**

**当前小程序问题**：只显示了 RMS、收缩力度、疲劳度 3 个卡片，漏掉了 MDF 原始值和独立时间信息（ts 只是用于时间同步）

**待办**：修改 `E:\Personal\sEMG\code\0421\pages\realtime\` 的 realtime 页面：
1. index.wxml — 改成 4 张卡片（RMS原始 / 收缩力度 / MDF原始 / 疲劳度）+ 时间戳一行
2. index.js — 解析 TCP JSON 中的 `m` 字段（MDF）并显示
3. 固件侧无需修改

---

## 2026-05-07 补充（06:52 北京时间）- 会话压缩前flush

### 实时页 5 字段显示修复（本次完成）

**问题根因**：`_onSample()` 中 `const { r: rms, f: mdf, ts } = d` — 把固件的 `f`（疲劳度）错误当成 MDF 使用，导致 MDF 字段从未显示。

**固件实际发送**：`{"ts":...,"r":0.12,"m":95.3,"f":0.23,"q":85,"a":0.67}`

**修改文件**（小程序侧，固件不动）：
1. `index.wxml` — 2×2 四卡片布局（RMS/收缩力度/MDF/疲劳度）+ 底部信息栏（时间戳+信号质量）
2. `index.js` — 修复字段映射 `r`=RMS, `m`=MDF, `f`=疲劳度, `a`=激活度, `q`=质量；新增 sparkMdf 迷你趋势线（蓝色 #60a5fa）；新增 timeStr 时间戳格式化
3. `index.wxss` — `.card-value.mdf { color: #60a5fa }` + `.info-bar` / `.info-text` 样式

**卡片最终布局**：
```
┌─ 实时 RMS ─┐  ┌─ 收缩力度 ─┐
├─ MDF ─────┤  ┌─ 疲劳度 ──┐
📡信号质量    🕐 HH:MM:SS
```

**文件尺寸**：`index.wxml`(2653B) / `index.js`(12789B) / `index.wxss`(3296B)

**开发记录**：`C:\Users\honghuang\.qclaw\workspace\realtime-5fields_20260507-0647.md`
---

## 2026-05-07 补充（06:54 北京时间）- 会话压缩前flush

### 实时页布局确认 — 用户纠正

**用户期望布局（重要！）：**
- 上半部：**横向排列 4 张卡片**（RMS / 收缩力度 / MDF / 疲劳度），纯数值+单位，无迷你趋势线
- 下半部：**两个大图表**（RMS曲线 + MDF曲线），各自叠加两条曲线
  - 实时曲线 = 当前采样
  - 个人拟合曲线 = A区校准数据生成的基线
  - B区标准曲线 = 用户从 B 区库调取的参考曲线
- 图表滚动显示最近 30 条数据

**之前做的布局（错误）：**
- 2×2 四卡片 + 4个spark迷你趋势线
- 把 MDF 单独卡片 + MDF迷你趋势线
- 用户不需要卡片里的迷你趋势线！

**图表方案（确认）：**
```
┌─ RMS ─┬─ 收缩力度 ─┬─ MDF ─┬─ 疲劳度 ─┐  ← 横向4卡片，无迷你线
├─────────────────────────────────────────┤
│        RMS 实时曲线图表                  │  ← 绿=实时, 灰=拟合, 蓝=B区
│        ← 滚动显示30条 →                  │
├─────────────────────────────────────────┤
│        MDF 实时曲线图表                  │  ← 蓝=实时, 灰=拟合, 绿=B区
│        ← 滚动显示30条 →                  │
└─────────────────────────────────────────┘
```

**技术关键点：**
1. B 区标准曲线需要小程序从固件 B 区读取（或通过协议请求下发）
2. 个人拟合曲线来自 A 区校准数据（小程序本地已有 calib_data）
3. 实时监控时固件只发单点数据，曲线叠加逻辑由小程序处理
4. **analysis 页仍是空文件**，待开发

**小程序代码路径**：`E:\Personal\SEMG\code\0421\pages\realtime\`
**固件路径**：`E:\Personal\sEMG\sEMG_Firmware_V1.4`

---

## 本次会话重要结论

### 用户核心需求澄清（2026-05-07）

用户使用微信小程序通过 WiFi（TCP）连接 sEMG 设备，需要**实时监控页面**同时显示：

**① 上半部 — 4个数值卡片（横向排列）：**
- 实时 RMS（肌肉激活程度）
- 收缩力度（百分比，已校准）
- MDF（肌电信号中心频率 Hz）
- 疲劳度（百分比，固件计算）

**② 下半部 — 两个波形图表（滚动30点）：**
- **RMS 图表**：实时 RMS 曲线 + 个人拟合曲线 + B区标准曲线
- **MDF 图表**：实时 MDF 曲线 + 个人拟合曲线 + B区标准曲线

**技术约束：**
- 固件 NetManager::sendData() 已发送全部数据点（ts, r, m, f, q, a）
- B 区标准曲线需通过协议从固件读取，或由小程序预存
- 个人拟合曲线来自 A 区校准数据
- 实时曲线叠加/滚动逻辑由小程序处理

### 小程序页面现状
| 页面 | 状态 |
|------|------|
| realtime（实时监控）| 需按新布局重构 |
| calibrate（校准） | ✅ 完成 |
| analysis（历史分析）| ❌ 空文件，待开发 |

### 固件现状
- TCP 推流：✅ 完整发送 6 字段
- B 区读取协议：❌ 未实现，需设计

### 09:16  -  实时页面改造与数据卡片布局

- 实时页面布局方案确定：上半部横向显示4张数据卡片（RMS/收缩力度/MDF/疲劳度），下半部两个独立图表分别显示RMS曲线和MDF曲线，各显示30条滚动历史数据
- 两个图表分开的原因：RMS和MDF量纲不同无法共用Y轴，RMS对比力量大小，MDF对比疲劳程度
- 图表曲线来源：个人拟合曲线来自B区个人曲线库，B区标准曲线从B区曲线库按条件调取，由小程序加载并显示对比
- 固件端需要同步修改sendData()发送格式，增加a（收缩力度）和y（疲劳度）字段，5个信息全部由固件计算后发送
- 小程序realtime页面需改为4卡片横向布局+双图表布局，需要读取B区个人曲线数据
- 用户倾向于实时页面横向显示4张数据卡片，下方分两个独立图表显示RMS和MDF各30条滚动数据

### 19:51  -  戴可行状态确认

- 用户委托戴可行处理微信小程序编译任务，需确认结果后反馈给用户
